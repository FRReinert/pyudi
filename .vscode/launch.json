{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Python: Entry program",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/pyudi/__init__.py",
            "console": "integratedTerminal",
            "env": { "PYTHONPATH": "${workspaceFolder}"}
        }
    ]
}